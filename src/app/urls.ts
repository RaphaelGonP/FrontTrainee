export class Urls {
    id:number;
    nome:string;
    url:string;

    constructor(id:number, nome:string, url:string){
        this.id = id;
        this.nome = nome;
        this.url = url;

    }
}